<div class="box-dash">
        <% @stones.each do |stone| %>
  <div class="dash-cards">
      <img class="dash-img" src="<%=stone.image%>" alt="<%=stone.name%> image">
    <div class="dash-card-content">
      <h5 class="name"> <%= link_to stone.name, stone_path(stone), class: "text-decoration-none" %></h5>
        <p class=""><%= stone.description %></p>
          <% if stone.user == current_user %>
          <a href="#"><%= link_to 'Modifier', edit_stone_path(stone), class:"button-6"%></a>
          <a href="#"> <%= link_to 'Suprrimer', stone_path(stone), data: {turbo_method: :delete, turbo_confirm: "Êtes-vous certain de vouloir supprimer votre offre?"}, class:"button-7"%></a>
          <%end%>
    </div>
  </div>
        <%end%>
</div>
 <%= link_to "Publier une offre", new_stone_path, class: "btn btn-primary" %>





<div class="box-dash">
          <% @stones.each do |stone| %>
  <div class="dash-cards">
    <img class="dash-img" src="<%= stone.image %>" alt="Card image cap">
      <div class="dash-card-content">
                <h5 class=""><%= stone.name %></h5>
                <% @bookings.each do |booking| %>
                <p class="card-text"> <%= booking.comment %></p>
                <p class="card-text"> <%= booking.status %></p>
                <p class="card-text"> <%= booking.start_date %></p>
                <p class="card-text"> <%= booking.end_date %></p>
      </div>
  </div>
               <% end %>
          <% end %>
</div>
